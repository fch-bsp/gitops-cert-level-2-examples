apiVersion: batch/v1
kind: Job
metadata:
  namespace: example06
  generateName: cleanup-after-sync-
  anotations:
    argocd.argoproj.io/sync-wave: PostSync,"20"
  labels:
    codefresh.io/cert: cleanup-job  
spec:
  template:
    spec:
      containers:
      - name: dummy-run
        image: alpine
        command:
          - "sleep"
          - "10"
      restartPolicy: Never
  backoffLimit: 2    
